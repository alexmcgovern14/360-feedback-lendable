# QA run – 2025-02-07

**Base URL:** https://360-feedback-lendable.vercel.app  
**Method:** HTTP fetch (no interactive browser automation available in session).

---

## Result: blocked by database connection

All main routes return **HTTP 200** but render **"Database unavailable"** because the app cannot reach the database at `db.pcjjjpydenieeonnfngn.supabase.co:5432` (direct connection, port 5432).

| Route        | HTTP | Content |
|-------------|------|--------|
| `/`          | 200  | Database unavailable (reviewCycle.findFirst) |
| `/employee`  | 200  | Database unavailable (reviewCycle.findFirst) |
| `/reviewer`  | 200  | Database unavailable (nomination.findMany) |
| `/manager`   | 200  | Database unavailable (reviewCycle.findMany) |

Error copy is correct and points to:
- Setting DATABASE_URL on Vercel (no quotes).
- Using Supabase **connection pooler (port 6543)** for production.

---

## Shell / UX (from fetched content)

- **Title:** "Lendable 360 Feedback".
- **Copy:** "Database unavailable" plus the Prisma error and the pooler hint.
- **Nav:** Not fully verifiable via fetch; assumed present from app structure (Employee, Reviewer, Manager).

---

## What could not be tested (needs browser or working DB)

- Sidebar and navigation behaviour.
- Employee: combobox, add reviewer, deeplinks, validation.
- Reviewer: inbox list, request page, form, chat, completion.
- Manager: cycle list, cycle detail, summary edit, finalise, omitted insights.

---

## Next steps

1. **Fix production DB:** In Vercel, set **DATABASE_URL** to the Supabase **connection pooler** URL (port **6543**). Redeploy.
2. **Re-run QA:** After redeploy, run **QA-checklist.md** in the browser (or re-enable browser automation and run again).
3. **Optional:** Ensure cursor-ide-browser MCP tools (`browser_navigate`, `browser_snapshot`, etc.) are enabled for the agent so future runs can do click/type/snapshot QA.

---

## Run 2 (same day – re-check)

**Method:** HTTP fetch (mcp_web_fetch) to `/`, `/employee`, `/reviewer`, `/manager`.

### Result: still blocked by database connection

Same error as Run 1. All routes return **HTTP 200** but render **"Database unavailable"**:

| Route        | Error source                  |
|-------------|-------------------------------|
| `/`          | reviewCycle.findFirst()       |
| `/employee`  | reviewCycle.findFirst()       |
| `/reviewer`  | nomination.findMany()         |
| `/manager`   | reviewCycle.findMany()        |

**Database target:** `db.pcjjjpydenieeonnfngn.supabase.co:5432` (direct, port 5432).

### Required fix

Set **DATABASE_URL** on Vercel to the Supabase **connection pooler** URL (port **6543**), not the direct URL (5432). Redeploy, then re-run QA.
